# GitExpose - Core Dependencies
# Cap <3.14: aiohttp 3.14 made ClientResponse.__init__ require `stream_writer`,
# which the current aioresponses (0.7.8) test mock does not pass — see test_scanner.
# Lift once aioresponses ships compatibility.
aiohttp>=3.9.0,<3.14
click>=8.1.0
colorama>=0.4.6
httpx>=0.27.0
cyclonedx-python-lib>=8.0.0
packageurl-python>=0.15.0
PyYAML>=6.0
tomli>=2.0.0; python_version < "3.11"

# Advanced Features (optional but recommended)
aiofiles>=23.2.0
GitPython>=3.1.40
rich>=13.7.0
# Optional: polyglot/extension-content-mismatch detection (needs libmagic system lib)
python-magic>=0.4.27

# Cloud Integration (optional)
# boto3>=1.34.0
# google-cloud-compute>=1.19.0
# azure-mgmt-compute>=30.0.0

# Shodan Integration (optional)
# shodan>=1.31.0
